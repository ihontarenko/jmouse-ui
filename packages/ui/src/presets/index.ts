/**
 * `@jmouse/ui/presets` — the shared theme catalogue, opt-in.
 *
 * The palette VALUES that go with it are `@jmouse/ui/presets/themes.css`; the two are separate
 * imports because a product may want the names and not the colours, or the colours and not the list.
 */

export {
  allDarkThemes,
  allLightThemes,
  allThemes,
  darkThemes,
  findPresetTheme,
  lightThemes,
  seasonalThemes,
} from "./themes"
